# Weather App (ITMO finals demo test)

Мобильное приложение для просмотра погоды, разработанное на Flutter.

## Возможности приложения

### Основной функционал
- Отображение названия города
- Отображение текущей погоды (температура, описание)
- Данные загружаются через OpenWeatherMap API

### Дополнительный функционал
- **Иконка погоды** - динамическая иконка погоды от OpenWeatherMap
- **Поиск города** - возможность искать погоду по названию города
- **Геолокация** - отображение погоды по текущему местоположению
- **Обработка ошибок сети** - уведомление при проблемах с сетью
- **Кэширование** - сохранение последних данных о погоде, уведомление о данных из кэша

## Инструкция по запуску

### Предварительные требования

1. **Flutter SDK** (версия 3.7.2 или выше)
   
   Установка Flutter:
   ```bash
   # Linux/macOS
   git clone https://github.com/flutter/flutter.git -b stable
   export PATH="$PATH:`pwd`/flutter/bin"
   
   # Или скачайте с официального сайта:
   # https://docs.flutter.dev/get-started/install
   ```

2. **Android Studio** или **VS Code** с Flutter плагином

3. **Android SDK** (для запуска на Android)
   - Минимальная версия API: 21 (Android 5.0)
   - Целевая версия API: 34 (Android 14)

4. **Эмулятор Android** или физическое устройство
   - Для физического устройства включите режим разработчика и USB-отладку

### Шаг 1: Проверка установки Flutter

Выполните команду для проверки корректности установки:

```bash
flutter doctor
```

Убедитесь, что все необходимые компоненты установлены (должны быть зелёные галочки).

### Шаг 2: Клонирование репозитория

```bash
git clone <URL_репозитория>
cd weatherapp_demo_application
```

### Шаг 3: Установка зависимостей

Выполните команду в корневой директории проекта:

```bash
flutter pub get
```

Эта команда установит следующие зависимости:
- `http` - для HTTP запросов к API
- `geolocator` - для работы с геолокацией
- `shared_preferences` - для локального кэширования данных
- `intl` - для форматирования данных

### Шаг 4: Подготовка устройства

**Вариант A: Запуск на эмуляторе**

1. Откройте Android Studio
2. Перейдите в Tools → Device Manager
3. Создайте новый виртуальный девайс или запустите существующий
4. Дождитесь полной загрузки эмулятора

**Вариант B: Запуск на физическом устройстве**

1. Подключите устройство по USB
2. Включите режим разработчика на устройстве:
   - Перейдите в Настройки → О телефоне
   - Нажмите 7 раз на "Номер сборки"
3. Включите USB-отладку:
   - Перейдите в Настройки → Параметры разработчика
   - Включите "Отладка по USB"
4. Подтвердите подключение на устройстве

Проверьте, что устройство обнаружено:
```bash
flutter devices
```

### Шаг 5: Запуск приложения

```bash
# Запуск в режиме отладки
flutter run

# Или запуск в release режиме (быстрее работает)
flutter run --release
```

Если подключено несколько устройств, выберите нужное:
```bash
flutter run -d <device_id>
```

### Шаг 6: Сборка APK (опционально)

Для создания установочного APK файла:

```bash
# Debug версия
flutter build apk --debug

# Release версия
flutter build apk --release
```

Готовый APK будет находиться в:
`build/app/outputs/flutter-apk/app-release.apk`

## 📖 Использование приложения

### Главный экран

При запуске приложение автоматически:
1. Пытается получить погоду по текущему местоположению
2. Если геолокация недоступна - загружает последний сохранённый город
3. Если кэш пуст - показывает погоду для Москвы

### Поиск города

1. Нажмите на иконку 🔍 в правом верхнем углу
2. Введите название города (на русском или английском)
3. Нажмите "Найти" или клавишу Enter

### Погода по местоположению

1. Нажмите на иконку 📍 в левом верхнем углу
2. Разрешите доступ к геолокации (при первом использовании)
3. Приложение автоматически определит ваш город

### Обновление данных

Потяните экран вниз (pull-to-refresh) для обновления данных о погоде.

## Конфигурация API

Приложение использует бесплатный API OpenWeatherMap. В текущей конфигурации используется тестовый API ключ.

Для использования собственного ключа:
1. Зарегистрируйтесь на https://openweathermap.org/
2. Получите API ключ в личном кабинете
3. Замените значение `_apiKey` в файле `lib/services/weather_service.dart`

## Структура проекта

```
lib/
├── main.dart                    # Точка входа приложения
├── models/
│   └── weather_model.dart       # Модель данных погоды
├── screens/
│   └── weather_screen.dart      # Главный экран приложения
└── services/
    ├── weather_service.dart     # Сервис для работы с API погоды
    ├── location_service.dart    # Сервис геолокации
    └── cache_service.dart       # Сервис кэширования
```

## Решение проблем

### Ошибка "flutter: command not found"
Убедитесь, что Flutter добавлен в PATH:
```bash
export PATH="$PATH:/path/to/flutter/bin"
```

### Ошибка при сборке Android
Выполните:
```bash
flutter clean
flutter pub get
flutter run
```

### Проблемы с геолокацией
- Убедитесь, что GPS включён на устройстве
- Предоставьте приложению разрешение на доступ к местоположению

### Ошибка сети
- Проверьте интернет-соединение
- При отсутствии сети приложение покажет данные из кэша

## 📄 Лицензия

MIT License

